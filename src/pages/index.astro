---
import Layout from "../layouts/Layout.astro";
import PostList from "../components/PostList.astro";

// トップ（`/`）。Next 版は `/posts` へリダイレクトしていたが、ここでは要件どおり
// `/` のアドレスのまま記事一覧を描画する（リダイレクトしない）。
---

<Layout>
  <PostList />
</Layout>
